#include <stdio.h> // include library wich contain printf function

int main(void)  // (void) because no argument needed
{
    printf("hello, world !\n");
    return (0); // return (0) because function main return a int
}
